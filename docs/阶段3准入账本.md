# 阶段 3 准入账本

本文记录阶段 2 中可以由契约、自动测试和测量直接决定的事项。只有需要主观画质判断、
Nsight 管理员权限或现有证据不足的项目才延后给阶段 3 决策；不得用尚未稳定的性能数据反推
精度或语义。

## 自动准入原则

- 精确身份、离散分类和拓扑事实必须保持整数或枚举语义；样本高水位只能决定当前物理编码，
  不能把逻辑身份上限改成样本上限。
- 连续量只有在全域性质测试或真实样本加解析误差上界同时证明误差不可见时才能收缩。
- 纹理源码域、页面布局、transport 数值和坐标分别测量，不能用一种数据的分布替另一种做决定。
- 测量格式只保存聚合值，不保存世界坐标、玩家身份或纹理像素；它不是长期兼容的生产格式。
- 任一迁移必须先有旧路径 oracle、误差门限和资源/时间基线，再切换生产消费者。

## 可重复运行时采集

运行时采集默认完全关闭。显式开启后，渲染线程每隔固定帧数采样 VMA，并将阶段 2 聚合值
原子更新到 `run/prime-renderer-measurements/latest.json`：

```powershell
$env:JAVA_TOOL_OPTIONS = "-Dprime.renderer.measure=true -Dprime.renderer.measure.intervalFrames=240"
.\gradlew.bat runClient '--args=--quickPlaySingleplayer 新的世界'
```

`prime.renderer.measure.intervalFrames` 的合法范围是 1–3600，默认 120。关闭开关时不会查询 game
目录、扫描源像素或访问文件系统。采集器只由 `VulkanRenderer` 的渲染线程拥有，不增加锁域。
目前记录：

- atlas、mip、TextureId 高水位与空洞、动画、最大 sprite/padding；
- 当前 block atlas 的 RGBA8 mip 理论字节；该值属于 Mojang 图像，不计入 Prime VMA；
- normal/optical 源数量、texel、帧数、各字节通道码域；
- 页面数、mip 总字节、基础层面积、占用矩形和 ABI 坐标高水位；
- MediumId renderer-lifetime 分配量与高水位；
- TLAS、唯一/实例化三角形、area light 和 light-tree 规模；
- render/display extent、重建 backend/quality、相机单帧位移峰值；
- VMA block/allocation 与 heap usage 的采样峰值。

聚合文件会被新运行覆盖。需要对比时复制到 `build/reports/renderer-data/world-samples/`；该目录
属于一次性本机报告，不进入版本库。

## 2026-08-31 初始样本

环境为 RTX 4090、Vulkan 1.4.351、驱动 616.56、DLSS RR Performance、显示 854×480、
render 427×240、视距 32。数字是该次固定出生点的运行时峰值，不是跨机器性能门槛。

| 项目 | Vanilla + `新的世界` | SPBR-21 + `新的世界` | 当前结论 |
|---|---:|---:|---|
| TextureId 数量/高水位/洞 | 1279 / 1279 / 0 | 3463 / 3463 / 0 | u16 可容纳样本但逻辑仍保留 24-bit；待覆盖大包与 reload |
| MediumId 数量/高水位 | 4 / 4 | 4 / 4 | 当前物理高位很低；身份仍保持 u32，不按样本截断 |
| normal / optical 源 | 0 / 0 | 971 / 1131 | missing-channel 必须继续是显式 availability |
| normal / optical 页面 | 1×1 默认页 | 各 2×2048² | 当前 u16 坐标余量极大；生产 packer 上限仍由 ABI 约束 |
| normal / optical mip 字节 | 各 4 B | 各 44,695,552 B | 页面格式优化有明确显存价值 |
| 动画 sprite / cache | 0 / 0 B | 49 / 13,678,192 B | 动画不能回退到逐帧完整页面重建 |
| normal RG 码域 | 空 | R 20–229，G 26–229 | 仍按 RG8 源编码解释，不误当世界法线 |
| optical RGBA 码域 | 空 | 四通道均触及 0/255，G/B 为离散码 | 分类与连续语义分开；不可整体转成同一浮点语义 |
| 唯一 BLAS 三角形峰值 | 20,844,714 | 17,458,506 | 几何/路径状态每字节都值得优化，但不得换精度 |
| VMA allocation 采样 | 约 3.74 GiB | 约 3.31 GiB | 仅作资源基线；需要多世界和高分辨率样本 |

额外资源上界样本：ModernArch 128x 产生 16384×8192×4 mip block atlas，HD Realistic 产生
32768²×5 mip block atlas。后者的 RGBA8 mip 理论存储约 5.33 GiB，且不在 Prime VMA 统计中。
因此 canonical base color 不得以长期并存第二张 atlas 的方式落地；应替换上游存储，或按实际
sprite 矩形分页并在 generation 发布前完整构建。

启动日志中的 Windows Perflib/OSHI、离线开发账号 401 和 Realms 警告与 Prime 数据采集无关；
Prime 未产生 validation、measurement export 或资源生命周期错误。

## 阶段 2 决策状态

| 工作项 | 自动门禁 | 状态 |
|---|---|---|
| exact MediumId 全链路 | ABI、实时/离线/shadow 行为测试 | 已完成 |
| TextureId/MediumId/页面/资源测量 | JSON 结构测试、真实 Minecraft 样本 | 已完成基线；已覆盖 vanilla、LabPBR 与 32768² atlas，reload 留作后续扩展 |
| transport 连续量范围 | GPU/CPU 聚合、非有限值计数、样本分位数 | 待实现 |
| visible motion/depth 范围 | 生产帧 GPU reduction、相机 cut 分类 | 待实现 |
| phase-specific typed views | 编译期类型、ABI 产物和 Shader 行为测试 | 待实现 |
| linear Rec.2020 canonical texture | 双路径像素 oracle、mip oracle、显存/时间测量 | 待实现 |
| NVIDIA 规范 motion/depth | RR/Streamline oracle，NRD/FSR adapter 测试 | RR visible motion 已拆离 transport scratch 并迁移 RG32F；其余待实现 |
| 可安全精度收缩 | 全域误差上界 + 样本覆盖 + 原生产路径对比 | 待测量后决定 |

### RR motion 第一切片

RR 的 trace-private `TransportScratch` 和重建期 `VisibleMotionUv` 已改为独立图像，主 motion 从
`RG16F` 升为 `RG32F`；reflection motion 继续保持 `RG32F`。原生 bridge ABI 升至 v10 并在调用
NGX 前验证精确格式。RTX 4090 上以 DLSS RR Performance、427×240 render / 854×480 display
连续运行 13,320 帧，未出现 NGX evaluate、Prime 或 Vulkan validation 错误。RR render-extent
图像账本由 135 B/pixel 增至 147 B/pixel：其中 8 B 是不再错误复用的 transport scratch，另 4 B
是主 motion 精度提升。该切片证明格式可执行，不替代下一步 motion 范围、重投影误差和 camera-cut
测量，也不批准压回 FP16。

## 已登记延后项

- Nsight register、occupancy、cache、memory transaction 与逐 pass GPU 时间由用户统一采集；当前不
  以 JMH 抖动或总体 FPS 猜测具体瓶颈。
- `translationBenchmark` 的 extreme fixture 两个 fork 出现约 0.82 ms 与 1.6 ms 两簇结果；在排除
  harness/JIT/后台负载前只记录为测量稳定性问题，不设性能阈值。
- 主观透明材质、极暗亮度细节和重建稳定性只在数值/图像 oracle 无法判定时进入阶段 3 反馈清单。
