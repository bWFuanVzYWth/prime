name: build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: microsoft
          java-version: '25'
          cache: gradle
      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew
      - name: Set up Vulkan SDK toolchain
        uses: jakoch/install-vulkan-sdk-action@37effcfa045411f8bfbbda26df2fd1b3bf3436fa # v1
        with:
          vulkan_version: 1.4.357.0
          install_runtime: 'false'
          cache: 'true'
      - name: Report shader tool versions
        run: |
          "$VULKAN_SDK/bin/spirv-val" --version
          "$VULKAN_SDK/bin/slangc" -version
      - name: Set up headless Vulkan compute
        run: |
          sudo apt-get update
          sudo apt-get install --yes mesa-vulkan-drivers vulkan-tools vulkan-validationlayers
          lvp_icd="$(find /usr/share/vulkan/icd.d -name 'lvp_icd*.json' -print -quit)"
          test -n "$lvp_icd"
          echo "VK_DRIVER_FILES=$lvp_icd" >> "$GITHUB_ENV"
          VK_DRIVER_FILES="$lvp_icd" vulkaninfo --summary
      - name: Build and test
        run: ./gradlew --no-daemon --stacktrace clean check shaderTest jacocoTestReport
      - name: Capture verification reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: prime-test-reports-${{ github.sha }}
          path: |
            build/reports/tests/
            build/reports/jacoco/
            build/reports/prime-shaders/
            build/reports/vulkan-validation/
            build/test-results/
          if-no-files-found: ignore
      - name: Capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          path: build/libs/*.jar
          if-no-files-found: error
          archive: false

  native:
    runs-on: windows-2025
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: microsoft
          java-version: '25'
          cache: gradle
      - name: Run native bridge tests
        run: ./gradlew --no-daemon --stacktrace nativeTest
      - name: Capture native test reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: prime-native-test-reports-${{ github.sha }}
          path: |
            build/reports/tests/nativeTest/
            build/test-results/nativeTest/
          if-no-files-found: ignore
